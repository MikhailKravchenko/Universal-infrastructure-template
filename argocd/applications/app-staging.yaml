# Пример ArgoCD Application для приложения (app) в окружении staging.
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://nexus.example.com/repository/helm-hosted/"
    chart: app
    targetRevision: "0.1.3"
    helm:
      parameters:
        - name: environment
          value: staging
        - name: backend.replicaCount
          value: "1"
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
